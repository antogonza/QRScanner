import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'es.antogonza.qrscanner',
  appName: 'QRScanner',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
